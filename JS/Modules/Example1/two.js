function multiply(a,b) {
    return a*b;
}

export let msg = 'Anything3223' // named export if we export like this 


export function divide(a,b) {       // name exprot 
    return a/b;
}

export default multiply;     // default type export. in a file(module) only one default module can live 